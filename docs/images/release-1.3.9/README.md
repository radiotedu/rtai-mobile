# Candidate image provenance

- Files ending `700fdf4.png`: unedited PNGs saved from the delivered production-signed APK. The recap shows the restored 16-minute total; the podcast Square shows the complete publisher name. These verify export layout, not release readiness.
- Files ending `8e459ff.png`: preceding candidate, with timezone and podcast title fixes. The Square podcast publisher was still truncated; the delivered candidate corrects this.
- Files ending `917f835.png`: earlier native exports, before the timezone fix. The displayed zero-minute recap was a discovered bug, not the user's true weekly total.
- `terminal.png`: actual 1.3.9 guest terminal session, rendered from captured ANSI output. Terminal source was unchanged by the final sharing redesign.
- Files containing `b14daf5` in their names are earlier candidate captures and are retained for provenance. They are not final-design or final-binary verification.

See [the verification report](../../RELEASE_1_3_9_VERIFICATION.md) before using these images. These files are not an approved final Google Play listing.
